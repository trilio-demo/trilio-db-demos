apiVersion: v1
kind: Secret
metadata:
  name: sqlserver-secret
type: Opaque
stringData:
  # SA_PASSWORD must meet SQL Server complexity requirements:
  # min 8 chars, uppercase, lowercase, digit, and special character
  SA_PASSWORD: "Demo1234!Strong"
  MSSQL_DATABASE: demodb
