apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  # Change these credentials before deploying in any non-demo environment
  POSTGRES_USER: demouser
  POSTGRES_PASSWORD: Demo1234!
  POSTGRES_DB: demodb
